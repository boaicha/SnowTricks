{% extends 'base.html.twig' %}

{% block title %}Mot de passe oublié{% endblock %}

{% block body %}
    {% for flash_error in app.flashes('reset_password_error') %}
        <div class="alert alert-danger" role="alert">{{ flash_error }}</div>
    {% endfor %}

    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="text-center mb-5">
                <h1 class="fw-bolder">Mot de passe oublié</h1>
            </div>
            <div class="card rounded-3 py-5 px-4 px-md-5 mb-5">
                <div class="card-body">
                    <div class="row gx-5 justify-content-center">
                        <div class="col-10">
                            {{ form_start(requestForm) }}
                            <div class="form-floating mb-3">
                                <input class="form-control" type="email" data-sb-validations="required"
                                    id={{ form_widget(requestForm.email) }}
                                    <label for="name">Email</label>
                            </div>
                            <div class="d-grid">
                                <button class="btn btn-primary btn-lg">Réinitialisation du mot de passe</button>
                            </div>
                            {{ form_end(requestForm) }}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
